# Base URL of the CollapseCast API (no trailing slash needed).
# Local development:
VITE_API_BASE_URL=http://localhost:8000
# Deployed (AWS App Runner service URL), set this as an environment variable in AWS Amplify Hosting:
# VITE_API_BASE_URL=https://<your-service-id>.<region>.awsapprunner.com
