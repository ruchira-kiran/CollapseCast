[pytest]
testpaths = tests
pythonpath = .
filterwarnings =
    # Emitted by Starlette's own TestClient module at import time, not by this project.
    ignore:The anyio.abc.BlockingPortal alias is deprecated:DeprecationWarning:starlette.testclient
